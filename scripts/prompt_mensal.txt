Hoje e dia 01 do mes -- rode o FECHAMENTO MENSAL de invasoes VW do grupo Apia, cobrindo o MES ANTERIOR inteiro (nao o mes atual). Voce esta na pasta do projeto (C:\Users\marcusrifai\Desktop\Coude\assobrav-invasoes).

IMPORTANTE: esta execucao e automatica e ninguem vai ler perguntas nem responder -- NUNCA pare para perguntar nada ao usuario. Se algo for ambiguo ou uma ferramenta nao estiver disponivel, tome a decisao mais razoavel sozinho e siga em frente ate completar o pipeline por inteiro. Terminar com um rascunho de e-mail (mesmo que imperfeito) e sempre melhor do que parar no meio sem nada.

PASSO 0 -- calcule as datas do mes anterior com precisao (nao faca de cabeca, rode isto):
    python -c "from datetime import date, timedelta; hoje=date.today(); ultimo_dia_anterior=hoje.replace(day=1)-timedelta(days=1); primeiro_dia_anterior=ultimo_dia_anterior.replace(day=1); print(primeiro_dia_anterior.isoformat(), ultimo_dia_anterior.isoformat())"
Isso imprime "<de> <ate>" (ex: "2026-08-01 2026-08-31"). Use esses dois valores em tudo abaixo. Chame de <data> o valor de <ate> (ultimo dia do mes anterior, ex: 2026-08-31) e de <AAAA-MM> o ano-mes desse periodo (ex: 2026-08).

1. Rode `python scripts/login_e_download.py --de <de> --ate <ate> --pasta fechamento_<AAAA-MM>`.
2. Rode `python scripts/processa_relatorio.py downloads/fechamento_<AAAA-MM>/invasao.xlsx reports/<data>.xlsx`.
3. Rode `python scripts/gera_dashboard.py reports/<data>.xlsx reports/<data>.html`.
4. Rode `python scripts/publica_github.py reports/<data>.html <data>`. Publica em https://marcusrifaiapia.github.io/assobrav-invasoes/<data>.html (fica no historico -- a pagina inicial so muda se <data> for mais recente que o ultimo relatorio diario ja publicado, o que normalmente NAO sera o caso aqui, e está certo assim). Se falhar (git, token, rede), NAO trave -- pule e siga usando o caminho local no e-mail.
5. Rode `python scripts/gera_email_html.py reports/<data>.xlsx reports/<data>_email.html`.
6. Rode `python scripts/cria_rascunho_gmail.py "Fechamento Mensal de Invasoes VW - Apia (<mes por extenso em portugues>/<ano>)" reports/<data>_email.txt reports/<data>_email.html`. Isso cria o RASCUNHO direto na pasta Rascunhos do Gmail via IMAP (nunca envia sozinho). NAO use a ferramenta create_draft do Gmail (MCP) -- ela normalmente nao esta disponivel nesta sessao do CLI; use sempre este script.

O passo 6 deve rodar sempre que os passos 1-3 tiverem sucesso, mesmo que o passo 4 falhe. So pule o rascunho normal se os passos 1-3 falharem de verdade (ex: login nao conseguiu, selector quebrou) -- nesse caso, nao invente dados: em vez disso, rode o passo 6 com um assunto/corpo avisando que o fechamento mensal falhou nesse passo, com a mensagem de erro.

Se algum DN aparecer sem grupo cadastrado em config/mapa_grupos.json, NAO pare -- deixe como "DN <codigo> (grupo nao cadastrado)" no relatorio e mencione isso claramente no e-mail, para o usuario cadastrar depois.
